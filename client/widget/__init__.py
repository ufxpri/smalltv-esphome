"""SmallTV Widget — a cross-platform (macOS + Windows) tray/menu-bar app that
supervises the SmallTV control panel.

Runs as a background agent with a status-bar icon: start/stop the panel server,
show whether it and the device are up, open the panel in a browser, and toggle
start-at-login. Everything else — sources, brightness, settings, the live
monitor — lives in the panel itself. See widget/README.md.
"""
__version__ = "0.1.0"
