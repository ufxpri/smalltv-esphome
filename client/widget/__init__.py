"""SmallTV Widget — a cross-platform (macOS + Windows) tray/menu-bar app that
controls a SmallTV-Ultra running this ESPHome firmware.

Runs as a background agent with a status-bar icon: switch pages, set brightness,
start/stop the live data bridges (stocks, PC stats), toggle start-at-login, and
open a settings window. See widget/README.md.
"""
__version__ = "0.1.0"
